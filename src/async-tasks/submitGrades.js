import delay from 'await-delay'


const submitGrades = async (store) => {
    await delay(1000)

    // const grades = [] // TODO: array for grades per question
    console.log('submitGrades') //, grades)
}

export default submitGrades
